#include <bits/stdc++.h>

using namespace std;

int main() {
    int N, M;  // 세로, 가로 크기
    int arr[9][9];
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < M; j++) {
            cin >> arr[i][j];
        }
    }

    return 0;
}